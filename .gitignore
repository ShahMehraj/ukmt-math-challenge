node_modules
dist
dist-ssr
*.local
.DS_Store
.env
build.log

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Unrelated sibling project that landed in the working tree
LlmSessionsDashboard/

# Local-only: UKMT copyrighted source PDFs + research notes (kept off the
# public Pages repo). Extraction scripts under scripts/ read these locally.
docs/
